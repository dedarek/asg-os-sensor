# OpenCode 1.18.25：待确认的最小观测插件安装计划

当前未安装、未修改任何 Agent 配置。本计划不授权执行；审批对象是下述测试工作区安装步骤。

## 证据与边界

本机只读 Info.plist 和 app.asar/package.json 确认桌面包 @opencode-ai/desktop 1.18.25，入口 out/main/index.js。进程树当前仅见 Electron Helpers，没有单独可确认的 CLI 引擎进程。
真实 Goose 调查引用 ev-1788950224205301000-221c995be9 等 MCP 证据，建议调查本地监听端口，明确协议/API/鉴权未知。这只是候选观察点，不是可用 Hook 的证明。

[OpenCode 官方插件说明](https://dev.opencode.ai/docs/plugins/) 描述项目 .opencode/plugins/ 目录、启动时加载，以及 tool.execute.before/after 扩展点。该文档说明产品接口，尚未证明本机桌面实例加载了同版引擎。因此以下是有条件计划，健康验证失败即停止，不扩大到全局安装。

## 拟作用文件

- 唯一插件：`/Users/mac/个人项目/asg-os-sensor-stage1/artifacts/stage1/opencode-hook-acceptance/.opencode/plugins/asg-observe.js`。
- 唯一输出：同测试工作区的 `asg-observe-events.jsonl`，权限 0600。
- 备份清单：独立 run-id 下记录以上文件原始哈希/存在性/权限；若插件同名已存在，先备份且不自动覆盖。
- 不更改全局配置、用户项目、app.asar、应用签名或任何已有插件；不加入 npm 依赖。

## 待用户确认后才实施的步骤

1. 实现并审查一个纯观测插件：启动健康事件，before/after 仅写 tool 名、会话/调用标识的散列、时间、结果是否完成；不写参数、命令、文件内容、模型消息、令牌。写盘异常应退出观测，不中断工具调用。
2. 在 OpenCode 打开上述测试工作区，重新启动该工作区对应引擎/会话。插件按启动加载；若仅重开会话未加载，再由用户安排桌面应用重启，先保护现有工作。
3. 验证启动健康事件对应实际引擎 PID+创建时间和本次随机 nonce；执行用户确认的一次无敏感数据测试操作，匹配一次 before/after。仅文件存在、端口可连、指纹命中均不算成功。
4. 测量事件丢失、重复、延迟与错误路径；确认无法记录敏感值。仅全部验证完成后，才将该实例标为“已验证生效”。
5. 卸载：仅移走本次创建插件，恢复同名文件备份及权限；重启同一引擎，再做一次测试确认本次 nonce 不再产生新事件。保留历史证据，不清空事件库。

## 能力及不能承诺的内容

拟验证工具调用前后观测。执行前决策仅作为未来接口可行性调查，本次插件不修改参数、不抛异常阻断、不承诺完整 MCP/Skill 覆盖。
未知：桌面实例实际使用的引擎版本/配置目录、插件加载范围、所有工具是否触发事件。上述不确定项未解决前，不应批准推广或全局安装。
