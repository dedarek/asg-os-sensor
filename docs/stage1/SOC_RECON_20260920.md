# SOC 直连与真实对账附录（2026-09-20 凌晨）

本附录补充 ACCEPTANCE_TABLE_20260919.md 之后的真实链路验收，全部数据来自本机 SOC POC（网关 8095 / 管理端 8094 / 页面 8096 / MinIO 59000 / PostgreSQL 55432）与终端采集器实跑记录。

## 通过项（本机证据）

1. 采集器常驻与上报恢复
   - 终端服务 com.asg.soc-collector（PID 46149，连续运行 >1.5 小时）；SOC 侧 asg_runtime_state 每个在线实例每 15 秒刷新，验收窗口 00:02:34 / 00:11:35 / 00:11:47 连续可见。
   - 此前 service.log 的 URLError 为 23:17 前旧记录，非当前状态。

2. 四类原生 Hook 包统一复用（目录选包，零模型调用）
   - 真实 Codex 实例 9601 自动安装 SOC 原生包 f232d067（transport=soc-native-v1，model_calls=0）。
   - 真实 OpenCode 实例 57879 自动安装 SOC 原生包 be5c3152（同 transport，model_calls=0）。
   - 真实 ZCode 实例 999 无兼容包，正确路由 needs_investigation -> protocol_then_goose，模型调用 0。
   - 以上由终端 discovery.soc_installation 自动完成，无手工步骤。

3. 新类型自动登记 + 自动安装 + 事件直报（端到端）
   - 23:57 新 OpenCode 实例 57879:1789830410.756945 被发现后自动注册为 asg-a42ce474289839f420ea59190f1d5c99，自动安装原生包，hook.loaded / user.input / model.request / model.response / tool.* / control.applied 直报 SOC。
   - 逐条对账：终端绑定日志 pid=57879 记录 2404 条 = SOC asg_runtime_events 该 Agent 2404 条（event_id 去重后 2404，缺失 0、重复 0，采集缓冲排空至 0）。

4. Codex 观察者 Hook 静默损坏修复（泛化性缺陷）
   - 根因：指纹库内嵌 hook.cjs 的进程祖先匹配正则被双重转义，2026-09-16 起 pid 解析全部 unresolved，事件仍写入但无法绑定实例——"看起来在采集、实际全部失效"的静默故障。
   - 处置：修复运行副本 /Users/mac/.codex/asg-observer/hook.cjs 与指纹库两份内嵌内容（expected_sha256 同步为新哈希），修复后 hook.loaded/user.input/tool.* 恢复带 pid。
   - 真实验证：修复后终端 9601 的 tool.execute.after 共 77 条，SOC 同一 Agent 77 条，逐条一致（00:19 检查）。

5. 学习包准入（候选 != 发布）
   - 管理端 setCurrentHookPackage 对 learned-* 一律 409（需安装兼容性与激活验证后才能提升）；学习包入目录 is_current=false，终端按构建摘要复核后方可安装。候选包不能被直接提升为共享发布包。

## 保留项（如实声明）

- 正常桌面 Codex 会话 userPromptSubmit 已 enabled+trusted（native_trust 探针确认），但修复后该会话由目标续跑驱动、尚无新的用户消息回合；下一次真实对话即产生 user.input 直报，10 回合逐字对账仍按原口径等待。
- AI Trust 真实平台接管、第二台机器复用、整机重启恢复：仍为待外部条件。
- 代码推送：gitlab.xpaas.lenovo.com 此刻 SSL 连接失败（VPN/内网未通），本地已提交，恢复后推送。
