# 本机修复与复验（2026-09-15）

## 本轮范围
修复本机验收与运行问题；不执行跨机器、机器重启或 AI Trust 平台接入。

## 已修复
- 确认测试固定至少 5 个有效样本；异常和样本不足不能通过。
- 迟到决策必须返回明确的“请求不再等待”错误；网络失败不算正确拒绝。
- 控制批次按目标实例隔离，目标变化后清除旧阶段结果。
- 汇总状态由最新子项计算，失败和本机未执行项不能被外部条件覆盖。
- 覆盖矩阵将实现索引与验收通过分开。
- 页面刷新间隔不再冒充实际显示延迟。
- 原生准入探测要求可执行文件与工作目录匹配，不向其他实例外推。
- 独立服务启动、发现已有绑定不再单独证明接管。
- 独立运行拒绝测试要求真实决策记录；工作负载工具数按本轮时间范围统计。
- 独立测试恢复服务时读取已保存配置，不写死扫描周期和调查配置。
- 网关在客户端断开后不再发送第二份 HTTP 响应，上游连接在结束时释放。
- 页面轮询只读取当前实例的 Hook 日志，历史绑定继续保留但不再拖慢首屏。
- 页面客户端取消旧请求时忽略 BrokenPipe/连接重置，不再污染服务日志。

## 实测证据
- control-confirm-batch.json：批准 5、拒绝 5、确认超时 5、服务中断 5、迟到决策 10、参数替换 5，通过。
- current-normal-conversation.json：当前正常 Codex 会话 3 条消息逐字一致，含 1 个完整回合；不足 10 回合，不签收完整批次。
- 独立运行补测结果以 independent-runtime.json 为准；本轮未重复 60 分钟耐久测试，旧报告已另存。
- 全量回归：61 个模块、404 个测试，全部通过。
- 页面服务冷启动：扫描期间接口 0.04–0.07 秒返回；首次扫描在 30 秒门槛内完成。最终页面展示 5 个 Agent 和 1 个相关基础设施；已退出的 WorkBuddy 不再残留。扫描间隔 3001 秒。
- 实时 Hook 查询使用近期有界窗口：512 KiB 查询约 0.18 秒、2 MiB 查询约 0.65 秒；20 轮采集延迟 P95/最大值均为 1.807 秒。页面实际可见延迟尚未用浏览器计时，因此仍未签收该项。
- Codex 原生 `user.prompt.submitted` 已统一计为 `user.input`；卡片显示“已捕获 2/6 类，仍有缺口”，与抽屉一致。
- 最终任务状态：任务 1/3/4 通过；任务 2/5/6 未执行完；任务 7/8 待外部条件。

## 尚未签收
- 正常桌面实例的 10 回合全链路、模型网络与实际控制。
- 浏览器实际更新延迟（刷新间隔不是测量结果）。
- WorkBuddy 和无原生 Hook 真实目标的完整批次。
- 本轮完整 60 分钟耐久测试。

原始结果保存在 artifacts/acceptance；通过状态不覆盖或删除上述缺口。
