# 2026-09-14 Hook 验证结果

本轮已在隔离的真实 OpenCode 实例上完成观测与执行控制验收。Hook 内容由 Goose 根据目标代码与接口证据生成，经通用安装器安装；安装文件与候选内容逐字一致。测试驱动没有预写或直接调用目标 Hook。

| 检查 | 实际结果 |
|---|---|
| 重启与复用 | 隔离实例重启，新 PID 自动按 exact 匹配绑定；安装 Hook 内容保持一致 |
| 拒绝执行 | 实际 write 工具进入 Hook，返回 deny，目标测试文件没有创建 |
| 允许执行 | 实际 write 工具收到 allow，文件创建且内容正确 |
| 等待确认 | 实际请求 pending 时文件不存在，页面审批接口拒绝后仍不存在 |
| 确认超时 | 真实请求在无人确认时超时拒绝，文件没有创建 |
| 原始事件 | 可查看用户输入、助手输出、模型请求侧参数、工具参数/结果、控制与会话事件 |
| 页面刷新 | Hook 事件独立于进程扫描周期刷新；3001 秒的扫描设置保留 |
| 自动化检查 | 317 项测试，4 项跳过，其余通过 |

完整模型网络请求与响应不属于本次已验证覆盖。模型请求侧参数事件不能等同于完整模型 HTTP 请求。其他 Agent 的安装记录不能继承此实例的验收结果；需要相应目标加载 Hook 后再验证。

本地详细报告由隔离测试目录保存，原始运行记录、凭据和已学习指纹不随源码发布。页面验收记录绑定 PID、创建时间、报告摘要与 Hook 文件摘要；实例退出或文件变化后失效。
