# 验收复核 2026-09-20

## 2026-09-20 下午补记（验收工具修复与心跳故障根因）

1. 正常实例对账工具修复：兼容 asg-io-v3 content 正文、续写会话多转录合并基准、
   评估窗口从首次采集到用户输入起算、无基准轮次与脱敏前缀单独计类。
   此前桌面实例被误报为 0 回合；修复后如实为窗口内 3 个完整回合、0 不一致，
   未满 10 回合判未达标（等待用户真实对话累积）。
2. 总表"正常用户实例独立验收"行现绑定 pid、instance_id、transport、hook 摘要
   与逐回合证据文件，不接受脱离实例的 passed 布尔。
3. 通道透明度：桥接事件带 channel=bridge，已装包直连带 channel=direct；
   SOC 库内可区分，direct 通道经真实网关协议验收入库。
4. 页面 Agent 离线根因修复：OpenCode 运行状态包 4.21MB 超网关 4MB 上限（413），
   桥接循环逐实例故障隔离并按预算裁剪（标注省略数量）。修复后三个存活实例
   心跳秒级新鲜。
5. 原生包匹配收紧：soc-native-v1 在目录提供平台/架构时一并校验。
6. 新增回归门槛 5 项（通道标记、体积裁剪标注、故障隔离、原生约束），
   SOC 集成套件 33 项全绿。beta/integrated-20260920 已更新；main 未动。
