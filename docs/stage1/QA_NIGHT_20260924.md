# QA 夜间找茬记录（2026-09-24 晚，分支 codex/qa-findings-20260925）

方法：先溯源（代码+日志+DB+真实调用），最小修复，单测+全量回归，双远端推送，热部署后真实端到端验证。全部改动只进 QA 分支，未触碰交付分支。

## 修复清单

| 编号 | 问题 | 根因 | 修复 | 验证 |
| --- | --- | --- | --- | --- |
| B14-B16 | （前段）实例事件文件重同步 / 通道盖章 / 预算裁剪 | 见各自提交 | 已合入 | 单测+回归 |
| B17 | retired 身份永久刷新上报 | 平台 dedup 标记 retired 后卡片永不回 online，客户端却继续心跳刷新 | 网关 runtime 回执带 agent_status；客户端收到 retired 判决即清退本地登记（key/登记/收据全删） | asg-8533 runtime 冻结于退休时刻，退役后 0 写入 |
| B18 | Goose 调查后守卫误伤 | digest() 瞬时 OSError 返回 None 被当成"目标已变"，烧掉整轮调查 | compatibility 不符允许最多 3 次复检；create_time 不符仍立即失败 | 回归通过 |
| B19 | 单条 >1.5MB 事件永久头阻塞 outbox | 网关 2MiB 上限整单 413，且尺寸选择永远保留组内第一条 | 超大行替换为有界 collection.gap 存根（保留原大小+摘要） | 新增单测；591 全绿 |
| B20 | 平台 fingerprint_import 命令是死路 | 桥接 PATHS 缺该路由 + 带体路由被拼 ?pid= 后缀 404 | 补路由 /api/recipe-bundle/import；pid 后缀守卫纳入该操作 | SOC 页面同款 API 真实下发 → completed，digest da0967bf… |
| B21 | 引擎 4xx 回执被记成 uncertain | HTTPError 落进 OSError 分支，掩盖了 4 起确定性路由缺陷 | <500 一律 failed 并带 http_status | 部署后重跑 scan_interval 全链路 completed |

## 核过无问题（记录证据，避免重复排查）

- 命令通道 7 类操作真实端到端：collect（17 秒后 SOC 出现新清点快照）、scan_interval（同值回写）、control_policy（管理员语义+非管理员 401）、trust_refresh、model_settings（引擎侧验证 succeeded）、verify、fingerprint_import。
- 资产唯一性：agent_resource_bindings 无 (agent,resource,installation) 重复；无孤儿资源/版本；retired 卡不再进入现役聚合（现役 6 agent 绑定）。
- 清点数量：Codex 27 技能 = 10（.agents 根实体）+17（.codex/skills）+1 嵌套，全部在 parts 中；skipped_symlink=27 为 guide 规定的根外软链跳过，实体在 larksuite 根以 27 条完整上报，无丢失、无跨根重复计数。
- 部署一致性：release 0.9.49-beta 与 QA 分支零漂移；网关二进制与 B17 提交同步构建。
- 事件表无新写入的原因：Codex observer 数据源自 09-23 停更的 events.jsonl（hook 已停用），非管道故障；DSH/WorkBuddy 事件通道当日写入正常。
- supervisor 中 Analyst Error/requests 模块告警全部为 09-22 历史行，当日为零。

## 遗留（需要产品决策或用户侧动作，非代码缺陷）

- DSH 正常实例未自动安装 SOC 包：fingerprint_reuse 的工作区解析依赖活进程打开文件证据，web 形态解析为 unresolved 后按设计拒绝猜测。方向：为 profile 根提供官方解析证据，而不是放宽 install_target。
- DSH 配方 cordis.patch.yml 的 expected_sha256 与两次学习时期内容均不一致（文件此后又被 legacy 清理改动）。属"配方沉淀未回写"旧病，修它必须走重新学习/重打包链路，不能手改摘要。
- Codex 桌面实例 Hook 信任、WorkBuddy 运行端点稳定性：等用户环境配合的验收项，维持原口径。
