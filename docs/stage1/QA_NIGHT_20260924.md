# QA 夜间找茬记录（2026-09-24 晚，分支 codex/qa-findings-20260925）

方法：先溯源（代码+日志+DB+真实调用），最小修复，单测+全量回归，双远端推送，热部署后真实端到端验证。全部改动只进 QA 分支，未触碰交付分支。

## 修复清单

| 编号 | 问题 | 根因 | 修复 | 验证 |
| --- | --- | --- | --- | --- |
| B14-B16 | （前段）实例事件文件重同步 / 通道盖章 / 预算裁剪 | 见各自提交 | 已合入 | 单测+回归 |
| B17 | retired 身份永久刷新上报 | 平台 dedup 标记 retired 后卡片永不回 online，客户端却继续心跳刷新 | 网关 runtime 回执带 agent_status；客户端收到 retired 判决即清退本地登记（key/登记/收据全删） | asg-8533 runtime 冻结于退休时刻，退役后 0 写入 |
| B18 | Goose 调查后守卫误伤 | digest() 瞬时 OSError 返回 None 被当成"目标已变"，烧掉整轮调查 | compatibility 不符允许最多 3 次复检；create_time 不符仍立即失败 | 回归通过 |
| B19 | 单条 >1.5MB 事件永久头阻塞 outbox | 网关 2MiB 上限整单 413，且尺寸选择永远保留组内第一条 | 超大行替换为有界 collection.gap 存根（保留原大小+摘要） | 新增单测；591 全绿 |
| B20 | 平台 fingerprint_import 命令是死路 | 桥接 PATHS 缺该路由 + 带体路由被拼 ?pid= 后缀 404 | 补路由 /api/recipe-bundle/import；pid 后缀守卫纳入该操作 | SOC 页面同款 API 真实下发 → completed，digest da0967bf… |
| B21 | 引擎 4xx 回执被记成 uncertain | HTTPError 落进 OSError 分支，掩盖了 4 起确定性路由缺陷 | <500 一律 failed 并带 http_status | 部署后重跑 scan_interval 全链路 completed |

## 核过无问题（记录证据，避免重复排查）

- 命令通道 7 类操作真实端到端：collect（17 秒后 SOC 出现新清点快照）、scan_interval（同值回写）、control_policy（管理员语义+非管理员 401）、trust_refresh、model_settings（引擎侧验证 succeeded）、verify、fingerprint_import。
- 资产唯一性：agent_resource_bindings 无 (agent,resource,installation) 重复；无孤儿资源/版本；retired 卡不再进入现役聚合（现役 6 agent 绑定）。
- 清点数量：Codex 27 技能 = 10（.agents 根实体）+17（.codex/skills）+1 嵌套，全部在 parts 中；skipped_symlink=27 为 guide 规定的根外软链跳过，实体在 larksuite 根以 27 条完整上报，无丢失、无跨根重复计数。
- 部署一致性：release 0.9.49-beta 与 QA 分支零漂移；网关二进制与 B17 提交同步构建。
- 事件表无新写入的原因：Codex observer 数据源自 09-23 停更的 events.jsonl（hook 已停用），非管道故障；DSH/WorkBuddy 事件通道当日写入正常。
- supervisor 中 Analyst Error/requests 模块告警全部为 09-22 历史行，当日为零。

## 遗留（需要产品决策或用户侧动作，非代码缺陷）

- DSH 正常实例未自动安装 SOC 包：fingerprint_reuse 的工作区解析依赖活进程打开文件证据，web 形态解析为 unresolved 后按设计拒绝猜测。方向：为 profile 根提供官方解析证据，而不是放宽 install_target。
- DSH 配方 cordis.patch.yml 的 expected_sha256 与两次学习时期内容均不一致（文件此后又被 legacy 清理改动）。属"配方沉淀未回写"旧病，修它必须走重新学习/重打包链路，不能手改摘要。
- Codex 桌面实例 Hook 信任、WorkBuddy 运行端点稳定性：等用户环境配合的验收项，维持原口径。
- ZCode 当前配方 hook.method=unsupported_no_safe_hook（Goose 21:18 调查结论）：目录无兼容包、下载 409、状态 needs_investigation 是链路一致的正确表现；若后续在 ZCode 新版发现可用挂接点，走 trial→重新学习→可安装包晋级链路，不改判定本身。

## B22 溯源结论（已核实为按设计，非缺陷，不改代码）

现象：ZCode 卡显示 no_compatible_SOC_package；GET /api/asg/artifact/catalog 返回空 items；下载 latest 返回 409。

溯源：全库 78 个 hook 包逐一解析——50 个可安装（direct/native），28 个 recipe-only（仅 recipe-bundle.json+agent-version.txt+README.txt）。网关 packageMetadata 对 recipe-only 包判 "not a SOC package"：catalog 静默跳过、download 409。这是有意设计：learned 配方包从未经过可安装性验证，只供 fingerprint_import 走复核导入；能进 catalog 供自动安装的只有 deployment_package.build 产物（validateInstallationArchive 强制 transport.json+install.py+runtime/*）或 SOC 原生包。学习配方与可安装包是两类制品，不能混为"已发布安装包"。

## 其他核实（避免误判为故障）

- scan_settings.json 当前 scan_enabled=false：用户手动扫描模式（既有偏好设置），周期扫描不跑、仅"立即采集/POST /api/scan"触发，属预期。手动扫描已验证可正常触发（23:59 一次完整扫描+匹配日志正常）。
- endpoint.log 尾部一批 URLError 为 22:56 网关重启窗口的瞬时记录，非当前状态；00:01 网关已正常受理该 key 的 catalog 请求。
- 8081 引擎在 scan_enabled=false 时长时间无 Scan Match 输出属预期，不是卡死（线程栈 sample 确认 poll 等待）。

## B23 fingerprints / fingerprint_import 命令端到端验收（00:11-00:14，通过）

- 管理下发端点溯源：AsgRuntimeController.java  POST /api/agent/{agentId}/asg/commands（8094，X-Test-User-Id: asg-poc-admin；operation 白名单含 fingerprints/fingerprint_import，fingerprint_import 需 admin 角色；instance 快照 60 秒内必须新鲜，否则 409）。
- 守卫验证：对无匹配指纹的 ZCode 下发 fingerprints 空参数 → 回执 failed: fingerprint_not_matched_to_target（按设计，只导出当前实例命中的配方）。
- 正向：对 Codex(asg-faebd) 下发 fingerprints 指定 harness-f4891e902ef6 → 回执 completed，result 为完整 asg-recipe-bundle.v1（integrity digest + compatibility，机器路径已剥离）。
- 闭环：将该 bundle 经 fingerprint_import 原样下发 → completed，status=needs_review、compatibility=unknown(no_observed_build)（参数无 observed_build/exe，引擎 /api/recipe-bundle/import 契约如此）。
- 全局库 harness-f4891e902ef6 条目未被污染（total 11 不变）。导入不落库是 import_bundle 设计：导入只做结构校验+兼容判断并给出待授权计划，写库仍由调查/复用链路负责；与前端提示一致。非缺陷。

## B24 Goose 强制复调查 ZCode 全程闭环验收（00:05-00:19，通过）

- 触发：POST :8081/api/reinvestigate?pid=15161 force=true；goose pid 51522，rc=0 completed，94 次工具调用，其中 propose_recipe 6 次。
- trial 语义正确：本次构建与库内 harness-2472c6a51dc6 兼容性不一致 → matcher 判 trial；调查按 trial 分支执行，
  强制 evolves_prior_harness 指向同族（不符会抛 Trial investigation must name the matched prior family），未创建新家族、未静默合并。
- 候选沉淀：onboarding experience.json 记录 investigation_recipe_saved(match_status=trial) + trial_candidate_saved
  （sha acf479ee…，00:19:25）；全局库条目保持 revision 1、hook_verified=False —— 候选须真实安装验证后才转 revision，符合设计。
- 结论一致：ZCode 3.14.3 仍无安全挂接点，plan.status=unsupported、action=manual_review、reason=配方接入适配器未登记: unknown；
  未尝试安装、未写目标文件。与 B22（recipe-only/不支持是正确表现）互证。
- 结论：陌生 Agent 调查→配方沉淀→trial 演进闭环在 Qwen3.8-Flash-Next xhigh 下可完整走通；ZCode 无可安装包属目标限制，非链路缺陷。

## B25 8081 引擎路由 → SOC 命令面迁移核对（纯 review，无缺口新增）

- 引擎全部管理路由（monitor_dashboard.py + runtime/hook_control.py）：/api/state /api/scan /api/scan-interval
  /api/reinvestigate(+/continue /cancel) /api/onboarding(/execute /verify) /api/native-trust(/refresh)
  /api/model-settings /api/fingerprints /api/recipe-bundle/{export,import} /api/hook-control/{status,policy,resolve,decide,ack}
  /api/hook-data(/download) /api/hook-events(/download) /api/investigation/activity /api/capability。
- SOC 命令桥 PATHS 共 14 个操作，逐一核对均命中真实引擎路由（含 B20 修复的 fingerprint_import、B21 的 4xx=failed 语义）。
- 前端 asg-runtime-section.vue 的按钮操作集（scan/investigate/continue/cancel/install/verify/trust_refresh/
  scan_interval/control_policy/control_resolve/model_settings/fingerprints/fingerprint_import）与 Java 白名单、PATHS 三方一致。
- 引擎独有路由的迁移状态：hook-data/hook-events 由桥接在 runtime 周期主动拉取并以 events 入库（等价覆盖）；
  /api/capability、/api/investigation/activity 为 8081 页面专用展示，SOC 侧由 asg_runtime_state/investigation 字段承接；
  完整 Hook 历史同步仍是既有遗留（记录在交接文档），非本晚新增缺陷。
- POST 防御核对：引擎管理写操作仅限本机连接（B28 本机守卫），局域网暴露只读，符合平台/终端边界设计。

## B26 种子指纹库重复条目 + 发布可移植性扫描漏检（缺陷，已修复）

- 现象1：交付种子库 runtime/fingerprints.json 里 @deepseek-ai/dsh 有 3 个条目
  （harness-18017d5a2dad / harness-14f726bfbc37 / harness-c2b044ce244d），其中
  harness-c2b044ce244d 与权威条目 harness-47823dd32ae8 features 完全相同但 method 不同
  （cordis_patch_plugin vs file_plan）→ exact 命中赢家不确定；且名称重复违反
  “一个 Agent 一份记录”。本机运行库早已去重（bak-dedupe-dsh），种子库一直没同步。
- 修复1：种子库删除 3 个重复/过时条目，保留每 Agent 唯一条目（9 条），
  特征碰撞与重名清零；测试无引用被删 ID（e2e 全部走 ASG_FINGERPRINT_DB 隔离库）。
- 现象2：sanitize_portability 用字面量 'C:\Users\' 匹配 json.dumps 后的 blob，
  dumps 会把反斜杠加倍（\ → \\），且 Windows 盘符大小写不敏感——正反两种大小写
  都永远匹配不上，学习机 home 路径照常进发布包（harness-03 的
  c:\users\yyyyc\... 即现证，B17 的修复当时就没生效）。
- 修复2：折叠双反斜杠 + 小写化后匹配 ':'+chr(92)+'users'+chr(92)；
  test_release_build_order 新增大小写两例回归（test_sanitizer_drops_lowercase_windows_home_paths）。
- 验证：目标 4 模块 42 tests OK；全量 592 tests OK（00:4x）。
  种子库变更只影响下次发布构建；运行引擎用 state/fingerprints.json，不受影响、无需热部署。


## B27 上线链路巡检（00:35-00:48，全部核过，无新缺陷）

- 心跳/在线语义核对：管理端 resolveStatus（AgentController.java:366-377）先按 3 分钟心跳，
  再用 60 秒 asg_runtime_state.received_at 兜底判在线。OpenCodex 卡（last_heartbeat 21:47）
  仍 online 属该设计：其 runtime 每 60s 实时写入（00:36:18 新鲜）。其心跳停更根因是进程被
  重新分类为 model_gateway（classification=infrastructure），discovery.confirmed 只放行
  pending/confirmed_agent —— 按设计不心跳，非缺陷。
- @larksuite/cli 卡 offline：进程已不存在（pgrep 无）、runtime 快照 09-23 10:08，
  状态如实，非僵尸误报。
- 8096 预览前端 API 冒烟（vite 代理 /soc/api/v1 → 8094）：agent-inventory、agent inventory、
  asg runtime、events 分页（50/页、sequence 严格降序、无重叠、instance_id 全有）、
  skill-tree、skill-file（真实 SKILL.md 内容）、versions（2 个内容版本）、
  版本内容下载（application/zip、可解压）全部 200 且语义正确。
- 端点 outbox 体检：queue/event_outbox/dead_letter/poison 全空；runtime_commands
  35 条中 failed/uncertain 均为 B20/B21 修复前旧记录（404 路由、instance 过期），
  修复后无新增。
- 现役引擎指纹库确为 state/fingerprints.json（launchctl com.asg.terminal 进程环境
  ASG_FINGERPRINT_DB 证实）；B26 种子修改只影响下次 release 构建与 seed 合并，无需热部署。
- retired 卡 889 行资源绑定历史：mergeAgents 设计即保留历史归属（注释与 moved jsonb 一致），
  维持不动，记录结案。

## B28 (2026-09-25 00:45, 核实通过 — P0 契约路径逐项验证)

按交接文档遗留项对 P0 契约实现做只读溯源核实（不修改）：

1. 首次不存在的根省略 vs 历史根删除：protocol.scope_present + collection_worker
   中四类目均走同一判断——路径不存在且 scope_key 不在 previous_scopes 时返回
   None（该根从本轮清单省略，不冒充空成功）；在 previous_scopes 中则返回
   success+空 items（真实的历史根删除信号）。测试
   test_learned_unknown_platform_and_removed_root 覆盖两分支。
2. 单根超时：collect_contract 总预算 60s；bounded_reader 取
   min(15, deadline-now) 起独立子进程，超时抛 INVENTORY_SCOPE_TIMEOUT 并
   只污染该 scope 状态（failed），类目级聚合为 partial/failed，其他根不受影响。
   skill_scope 内部 walk/哈希循环逐块检查 deadline。测试覆盖
   bounded_reader 超时路径（test_channel_and_budget）。
3. 先采集缓存后上报：endpoint.enqueue 在触网前先落 drafts（bounded 首轮快照），
   注册与 manual 路径用 enqueue_cached_drafts 重放已缓存草稿——离线首扫的
   Agent 即使进程退出，恢复联网后仍完整上报，不需要重新深扫。
4. workspace_id 规则：codex/opencode 取项目链最顶层 git 根、无 git 落
   nogit:cwd；hermes 取 HERMES_HOME 或 ~/.hermes；其余取 cwd。与 guide 一致。

结论：交接文档所称"P0 契约细节未闭环"中的这四项在当前代码已落地且有测试；
维持只核实、不改动。

## B29 (2026-09-25 00:47, 巡检通过 — 端点日志与实时链路)

- endpoint.log 自 23:30 起无任何新告警（文件 mtime 23:30）。
- 23:30 窗口的 URLError 簇与 'Local enrollment retired asg-8533…' 均为
  当时部署 0.9.49-beta 重启端点、网关瞬时不可达所致；retired 回执为 B17
  设计路径（平台已退役该测试卡，本地停报）。
- 现役 5 卡 asg_runtime_state.received_at 全部为 00:42:5x（秒级实时）。
- 现役进程：supervisor 41899 / engine 41906 / endpoint 41907，
  release 0.9.49-beta。巡检结论：无新缺陷。

## B30 (2026-09-25 00:53, 结案为按设计 — 事件库跨卡重复 event_id)

- asg_runtime_events 共 48155 行、45748 个不同 event_id；2407 个 event_id 出现在
  2 张卡下。逐组核验：所有多卡组的首卡均 retired（SQL 断言
  status<>'retired' 组数 = 0），抽样两组均为同一物理实例重新注册前后的
  Codex 旧卡（ccf0f7f5/48bcc576，皆 retired）。
- 唯一约束为 (agent_id,event_id)：事件按卡去重、跨卡允许同内容重放，
  与退役-重注册生命周期一致；在线 5 卡之间无任何重复。
- 事件读取接口按 agent_id+sequence 分页（AsgRuntimeController.events），
  不受影响。结论：历史重注册重放，非缺陷。

## B31 (2026-09-25 00:55, 巡检通过 — 全链路快照)

- 网关日志 9.9MB 持续写入，近 300 行无 4xx/5xx、无 error；00:50 仍在正常
  处理 GetByAPIKey/artifact catalog 请求。
- 引擎日志尾部为 B24 已结案的 Goose ZCode 调查记录（候选配方
  harness-2472c6a51dc6 trial），无新异常。
- 端点日志 23:30 后零新增（见 B29）。
- 快照一致性：agent-inventory 返回 6 卡（5 在线 + 1 如实 offline），
  agent_id 无重复；skill 67 条、model 5 条与逐项 assets 详情一致；
  agent_resources skill 总量 288 为全部历史卡累计（retired 保留归属，B27 设计）。
- 全量回归留档：592 tests OK（EXIT=0，47.2s，/tmp/qa_reg2.log）。
