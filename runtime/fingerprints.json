{
 "fingerprints": [
  {
   "id": "harness-01",
   "name": "codex",
   "first_seen": "2026-09-08T14:27:43",
   "match_count": 528,
   "mount_ms": 35000,
   "features": {
    "exe": "node.exe",
    "runtime": "node",
    "flags": [],
    "config_dirs": [],
    "entry_token": "codex.js"
   },
   "children_classes": [],
   "hook_recipe": {
    "match_features": {
     "argv_shape": [
      "<value>",
      "codex.js"
     ],
     "parent_lineage": [
      "powershell.exe",
      "WindowsTerminal.exe"
     ],
     "runtime_class": "node",
     "cwd_class": "unavailable",
     "child_count": 1,
     "descendant_count": 1,
     "child_argv_shapes": [
      [
       "<value>"
      ]
     ],
     "child_parent_lineage_contains": [
      "node.exe"
     ],
     "script_token": "[REDACTED]",
     "config_candidates_in_argv": [],
     "open_path_shapes": [],
     "network_shape": [],
     "stream_available": true,
     "stream_empty": true,
     "help_surface": {
      "line_count": 437,
      "has_json": true,
      "has_stream": false,
      "has_tool": false
     },
     "version_surface": {
      "line_count": 1
     }
    },
    "agent_identity_name": "codex",
    "observation": "Passive read-only observation only. PID 32680 is an active agent-like runtime: status running (create_time 1788846845.136483) with a live 1-descendant worker tree (PID 4376 spawned ~0.8s later), executing the codex.js CLI entry script (argv shape ['<value>', 'codex.js']) and launched from a terminal shell (parent lineage powershell.exe -> WindowsTerminal.exe) rather than a desktop IDE harness, so it is a standalone agent. Runtime class is Node.js: the target hosts a node.exe-lineage worker child (child parent_lineage contains node.exe), and the read-only probe of the observed executable returned a 437-line --help surface (has_json=true, has_stream=false, has_tool=false) and a 1-line --version, both returncode 0, consistent with a Node-hosted CLI agent with machine-readable output support. The child (PID 4376) is the native codex worker binary (prior-memory fingerprint harness-02: package path @openai/codex, tool-capable 132-line help, ESTABLISHED outbound TCP) - the agent's backend/worker subprocess. The primary semantic output surface is the supervisor-exposed stream (available but currently empty: 0 lines, 0 JSON); monitor it for emergent text/JSON lines. Configuration location: no config candidates in argv and no open path shapes (open_path_shapes empty); cwd_class is unavailable, so configuration is not reachable through the observed surface without secrets exposure and was not read. Protocol: terminal CLI with a JSON-capable help surface and a native worker subprocess holding an outbound connection; the supervisor-exposed stream is the passive observable channel. The target is an active codex agent runtime, currently idle on its output stream.",
    "hook": "Least invasive: no injection, no command execution, no file modification, no signal delivery, no memory attachment, no network interception, no reading of any config content. Use a passive stream tap on the supervisor-exposed stream that parses text/JSON lines as they appear, plus periodic read-only re-observation of the runtime surface and tree to detect newly appearing open paths, config candidates, children, or network transitions. The read-only --help/--version probes are the only executable interaction and must not be used to trigger side effects. No network sockets are open on the target surface; the child's ESTABLISHED outbound connection must not be probed or connected to. If an active hook is later required, the JSON-capable help surface and the native worker child indicate a possible protocol adaptation point, but the Analyst does not activate any hook; the Supervisor verifies and activates.",
    "redaction": "Redact all credential-like material (API keys, tokens, cookies, private keys, auth headers, session material) from any stream/help samples and from any config content. Do not read or expose any config values or the child's connection addresses/ports. Redact the raw argv <value> tokens and raw executable paths. Preserve non-sensitive structural tokens and shapes: the 'codex.js' script token, the node.exe lineage shape, the powershell.exe/WindowsTerminal.exe parent lineage, line counts (437 help, 1 version), the JSON/stream/tool capability booleans, path classes, and the empty open_path/network/config-candidate shapes.",
    "confidence": 0.8,
    "fallback": "If the stream remains empty and no configuration surface is observable, rely on the codex.js script token, the node.exe worker child lineage, the terminal parent lineage, and the --help/--version probe shapes (437-line JSON-capable help, 1-line version) to characterize the runtime as an active codex CLI agent. If the stream later emits output, parse text/JSON lines consistent with the JSON-capable CLI surface. If the target stays silent, classify it as an idle agent and re-observe the tree/runtime surface on a later cycle; if the target terminates, re-scan for a respawned process sharing the ['<value>', 'codex.js'] argv shape and node.exe lineage. If identity cannot be confirmed from distinguishing script/config tokens, keep the structural identity 'codex' derived from the codex.js script token and the node worker subprocess model."
   },
   "observed_exe_full": "C:\\Users\\yyyyc\\AppData\\Local\\hermes\\node\\node.exe",
   "last_seen": "2026-09-08T16:05:56"
  },
  {
   "id": "harness-02",
   "name": "hermes-agent",
   "first_seen": "2026-09-08T14:42:48",
   "match_count": 5,
   "mount_ms": 35000,
   "features": {
    "exe": "python.exe",
    "runtime": "python",
    "flags": [
     "-m"
    ],
    "config_dirs": [],
    "entry_token": "hermes_cli.main"
   },
   "children_classes": [
    "agentgateway.exe",
    "bash.exe",
    "cmd.exe",
    "conhost.exe",
    "copilot.exe",
    "node.exe",
    "python.exe"
   ],
   "hook_recipe": {
    "match_features": {
     "argv_shape": [
      "hermes-agent/python.exe",
      "-m",
      "hermes_cli.main",
      "<value>",
      "<value>"
     ],
     "parent_lineage": [],
     "runtime_class": "unknown-runtime",
     "cwd_class": "local-workspace",
     "child_count": 2,
     "descendant_count": 36,
     "config_candidates_in_argv": [],
     "open_path_shapes": [
      {
       "extension": ".log",
       "depth": 8,
       "is_config_like": false
      }
     ],
     "network_shape": [],
     "stream_available": true,
     "stream_empty": true,
     "help_surface": {
      "line_count": 43,
      "has_json": false,
      "has_stream": true,
      "has_tool": false
     },
     "version_surface": {
      "line_count": 1
     }
    },
    "agent_identity_name": "hermes-agent",
    "observation": "Passive read-only observation only. PID 49632 is an active agent-like runtime: status running, Python-hosted (hermes-agent/python.exe executing the hermes_cli.main module via -m), with an empty parent_lineage (no desktop IDE/harness executable in the lineage), so it is a standalone agent CLI rather than a worker under a host platform. It is a rich orchestrating host: 2 direct children and 36 descendants including a re-invocation of hermes_cli.main (PID 68804), an npx-cli.js node launcher, a tools/asg_graph.json graph server (--port 8091 --no-browser), an agentgateway.exe gateway, headless browser automation workers (--headless --width --height --signal --server), mini_bridge.py and monitor_dashboard.py helpers, and an ACP-over-stdio worker chain (npm-loader.js --acp --stdio -> node worker --acp --stdio). The semantic output surface is the supervisor-exposed stream (available but currently empty: 0 lines, 0 JSON); monitor it for emergent text/JSON lines. The --help probe returned 43 lines with has_stream=true (stream-oriented output) and no JSON/tool flags, and --version returned 1 line, both exit 0, indicating a Python CLI agent with a stream-based output surface. No configuration surface is observable: config_candidates_in_argv is empty, open_path_shapes contains only a non-config .log file at depth 8, network_shape is empty, and cwd_class is local-workspace, so no config file location, settings flags, or open config paths are exposed through the observed surface; configuration is not reachable without reading secrets and was not read. Protocol: the target is a standalone Python agent CLI (hermes_cli.main) with a stream-oriented help surface; its descendants include ACP-over-stdio workers, but the target itself is the host agent. The supervisor-exposed stream is the passive observable channel. The target is an active agent runtime, currently idle on its output stream.",
    "hook": "Least invasive: no injection, no command execution, no file modification, no signal delivery, no memory attachment, no network interception, no reading of any config content. Use a passive stream tap on the supervisor-exposed stream that parses text/JSON lines as they appear, plus periodic read-only re-observation of the runtime surface and tree to detect newly appearing open paths, config candidates, children, or network transitions. The read-only --help/--version probes are the only executable interaction and must not be used to trigger side effects. No network sockets exist in the observed surface, so there is no socket to avoid probing. If an active hook is later required, the stream-oriented help surface (has_stream=true) and the ACP-over-stdio worker descendants indicate possible protocol adaptation points, but the Analyst does not activate any hook; the Supervisor verifies and activates.",
    "redaction": "Redact all credential-like material (API keys, tokens, cookies, private keys, auth headers, session material) from any stream/help samples and from any config content. Do not read or expose any config values. Redact the raw argv <value> tokens and raw executable paths. Preserve non-sensitive structural tokens and shapes: the 'hermes-agent/python.exe' and 'hermes_cli.main' module tokens, the '-m' flag, the descendant script tokens (npx-cli.js, tools/asg_graph.json, agentgateway.exe, mini_bridge.py, monitor_dashboard.py, npm-loader.js), the '--acp'/'--stdio' protocol flags, line counts (43 help, 1 version), the JSON/stream/tool capability booleans, path classes, the .log open-path shape, and the empty config-candidate/network shapes.",
    "confidence": 0.78,
    "fallback": "If the stream remains empty and no configuration surface is observable, rely on the 'hermes-agent/python.exe' executable token, the 'hermes_cli.main' module token, the '-m' flag, the empty parent lineage (standalone agent), the rich descendant tree (graph server, gateway, ACP workers, dashboards), and the --help/--version probe shapes (43-line stream-capable help, 1-line version) to characterize the runtime as an active standalone Python agent CLI. If the stream later emits output, parse text/JSON lines consistent with the stream-oriented CLI surface. If the target stays silent, classify it as an idle agent and re-observe the tree/runtime surface on a later cycle; if the target terminates, re-scan for a respawned process sharing the ['hermes-agent/python.exe', '-m', 'hermes_cli.main', ...] argv shape. If identity cannot be confirmed from distinguishing script/config tokens, keep the structural identity 'hermes-agent' derived from the hermes-agent/python.exe executable token and the hermes_cli.main module token."
   },
   "observed_exe_full": "C:\\Users\\yyyyc\\AppData\\Local\\hermes\\hermes-agent\\venv\\Scripts\\python.exe",
   "last_seen": "2026-09-08T16:05:56"
  },
  {
   "id": "harness-03",
   "name": "WorkBuddy / sheetagent",
   "first_seen": "2026-09-08T14:42:49",
   "match_count": 184,
   "mount_ms": 35000,
   "features": {
    "exe": "node.exe",
    "runtime": "node",
    "flags": [],
    "config_dirs": [
     ".sheetagent"
    ],
    "entry_token": "start.mjs"
   },
   "children_classes": [
    "conhost.exe"
   ],
   "hook_recipe": {
    "match_features": {
     "argv_shape": [
      "<value>",
      "sheetagent/start.mjs"
     ],
     "parent_lineage": [
      "WorkBuddy.exe",
      "WorkBuddy.exe",
      "WorkBuddy.exe"
     ],
     "runtime_class": "unknown-runtime",
     "cwd_class": "local-workspace",
     "child_count": 1,
     "descendant_count": 1,
     "child_argv_shape": [
      "<value>",
      "<value>"
     ],
     "child_parent_lineage_contains": [
      "node.exe"
     ],
     "script_token": "[REDACTED]",
     "config_candidates_in_argv": [],
     "open_path_shapes": [
      {
       "extension": ".log",
       "depth": 6,
       "is_config_like": false
      },
      {
       "extension": ".mui",
       "depth": 9,
       "is_config_like": false
      }
     ],
     "network_shape": [],
     "stream_available": true,
     "stream_empty": true,
     "help_surface": {
      "line_count": 437,
      "has_json": true,
      "has_stream": false,
      "has_tool": false
     },
     "version_surface": {
      "line_count": 1
     },
     "prior_memory_fingerprint": "matches committed fingerprint harness-08 'WorkBuddy / sheetagent' (same argv shape, WorkBuddy.exe lineage, node.exe child shape, .log/.mui open paths, empty network, 437-line JSON-capable help, 1-line version)"
    },
    "agent_identity_name": "WorkBuddy / sheetagent",
    "observation": "Passive read-only observation only. PID 38652 is an active agent-like runtime: status running, Node-hosted (executing the ESM entry 'sheetagent/start.mjs' with a live node.exe child at PID 44600, argv shape ['<value>','<value>'], 0 grandchildren), launched by the WorkBuddy.exe host harness (parent lineage ['WorkBuddy.exe','WorkBuddy.exe','WorkBuddy.exe']). Because the target is a worker/sub-agent spawned by a host platform harness (WorkBuddy.exe appears in the parent lineage), the identity is synthesized hierarchically as 'WorkBuddy / sheetagent' from the host executable token 'WorkBuddy' and the script token 'sheetagent/start.mjs'. The semantic output surface is the supervisor-exposed stream (available but currently empty: 0 lines, 0 JSON); monitor it for emergent text/JSON lines. The --help probe returned 437 lines with has_json=true (JSON-capable CLI output) and no stream/tool flags, and --version returned 1 line, both exit 0, indicating a JSON-capable CLI worker. No configuration surface is observable: config_candidates_in_argv is empty, open_path_shapes contains only a non-config .log (depth 6) and a non-config .mui (depth 9), and network_shape is empty, so no config file location, settings flags, or open config paths are exposed; configuration is not reachable without reading secrets and was not read (prior memory infers a '.sheetagent' config dir from the script token, but this is not directly observed and must not be read). Protocol: the target is a Node.js CLI worker with a JSON-capable help surface and no protocol flags (no --acp/--stdio, unlike other fingerprints); the supervisor-exposed stream is the passive observable channel. The target is an active WorkBuddy-hosted sheetagent worker, currently idle on its output stream.",
    "hook": "Least invasive: no injection, no command execution, no file modification, no signal delivery, no memory attachment, no network interception, no reading of any config content. Use a passive stream tap on the supervisor-exposed stream that parses text/JSON lines as they appear (has_json=true indicates JSON-capable output), plus periodic read-only re-observation of the runtime surface and tree to detect newly appearing open paths, config candidates, children, or network transitions. The read-only --help/--version probes are the only executable interaction and must not be used to trigger side effects. No network_shape is exposed and no socket is observed, so there is no socket to probe; do NOT connect to any host/port. If an active hook is later required, the JSON-capable help surface (has_json=true) and the node.exe child (PID 44600) indicate a possible protocol adaptation point, but the Analyst does not activate any hook; the Supervisor verifies and activates.",
    "redaction": "Redact all credential-like material (API keys, tokens, cookies, private keys, auth headers, session material) from any stream/help samples and from any config content. Do not read or expose any config values (including any inferred '.sheetagent' config content). Redact the raw argv <value> tokens and raw executable paths. Preserve non-sensitive structural tokens and shapes: the 'sheetagent/start.mjs' script token, the WorkBuddy.exe parent lineage, the node.exe child lineage, line counts (437 help, 1 version), the JSON/stream/tool capability booleans, path classes, and the empty open_path/network/config-candidate shapes.",
    "confidence": 0.8,
    "fallback": "If the stream remains empty and no configuration surface is observable, rely on the 'sheetagent/start.mjs' script token, the WorkBuddy.exe parent lineage, the node.exe child lineage, and the --help/--version probe shapes (437-line JSON-capable help, 1-line version) to characterize the runtime as an active WorkBuddy-hosted sheetagent Node.js CLI worker. If the stream later emits output, parse text/JSON lines consistent with the JSON-capable CLI surface. If the target stays silent, classify it as an idle worker and re-observe the tree/runtime surface on a later cycle; if the target terminates, re-scan for a respawned process sharing the ['<value>','sheetagent/start.mjs'] argv shape and WorkBuddy.exe lineage. If identity cannot be confirmed from distinguishing script/config tokens, keep the structural identity 'WorkBuddy / sheetagent' derived from the sheetagent/start.mjs script token and the WorkBuddy.exe host lineage."
   },
   "observed_exe_full": "C:\\Users\\yyyyc\\.workbuddy\\binaries\\node\\versions\\22.22.2-2\\node.exe",
   "last_seen": "2026-09-08T16:05:56"
  }
 ],
 "version": 1
}