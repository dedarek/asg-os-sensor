# 协议接入与跨平台部署发布 · 2026-09-15

本版基于 GitLab `f0cb178` 整理当前工程实现，新增通用协议发现、ACP stdio 桥接、OTLP/HTTP 接收与自动部署入口。默认模型配置改为待填写、TLS 校验开启；默认指纹库为空。未带入开发机运行日志、真实会话、模型凭据或学习指纹。

## 主要变更

- JSON/JSONC/TOML 结构发现，目标关联配置引用与边界过滤；兼容命令式 Hook 保留原回调、事务安装、回滚、等待真实自检，失败继续 Goose。
- 已声明 `agent_servers` 的 ACP 启动包装；真实握手后登记独立子进程，转发会话与工具消息、拼接流式助手输出；权限请求同步决策。取消/中断不算完整输出。
- 带认证的 OTLP/HTTP JSON/protobuf logs/traces 接收，严格绑定活动实例和时间；GenAI input/output messages 内容提取；遥测不授予控制能力。
- Windows 文件锁、实例文件名、命令参数与路径处理；观察绑定更新加跨进程锁。
- setup.sh / setup.ps1 / setup.bat 与 deploy.py：隔离环境、依赖与 Goose 准备、配置、后台启动、状态、诊断、停止及重启；离线部署参数。
- 补齐输入缺失的对账失败记录；保留现有输入输出、独立服务、控制及配方复用实现。

## 本次执行结果

| 检查 | 结果 |
|---|---|
| Python 3.12.14 / macOS 全量单元与集成回归 | 432 项通过 |
| 最后一次 ACP 主绑定调整后的相关回归 | 17 项通过 |
| 全新安装、中文空格路径、依赖检查、HTTP 健康、重复启动不增进程、停止、重启 | 7/7 通过 |
| 本机已安装 OpenCode 的真实 ACP initialize | protocolVersion=1，加载事件已记录；退出码 0 |
| Windows 原生运行 | 未执行：仓库无关联 Runner，已提供可启用作业 |
| Linux 原生运行 | 本次未执行：GitLab 无关联 Runner |
| 真实产品完整聊天、全工具阻断和每种重启方式 | 本次不重新宣称通过；保留按实例验收 |

全量测试中的部分历史夹具有未关闭管道 ResourceWarning，不计作产品运行验收。协议测试使用匿名协议夹具；真实 OpenCode 批次只发送初始化，没有发送聊天或工具请求。

安装教程：[docs/DEPLOYMENT.md](docs/DEPLOYMENT.md)。自动安装不会把配置存在当作激活；ACP 不接管任意既有桌面会话；OTLP 不保证目标导出了完整正文。本版不宣称任何 Agent 都已完整接入。
